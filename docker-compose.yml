version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        REACT_APP_ENV: ${REACT_APP_ENV}
        REACT_APP_API_URL: ${REACT_APP_API_URL}
    ports:
      - "${PORT:-6103}:80"
