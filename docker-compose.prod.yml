services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        REACT_APP_ENV: production
        REACT_APP_API_URL: http://api.showbizbd.com
    ports:
      - "6103:80"
